# 09-v2.1-Automation.md

Cross-reference: `docs/PLATFORM-FEATURES-V2.md` v2.1 section.

## Workflow Automation

- [ ] Design and implement `Workflow` entity chaining triggers → actions (move/rename/compress/convert/notify/open-folder/delete-original).
- [ ] Implement command/event architecture formalization (builds on MVP's `BatchRecord` log — see `docs/TECH-STACK.md`).
- [ ] Workflow execution engine (runs chained actions, reports progress per step).

## Visual Rule Builder

- [ ] Design visual IF/AND/THEN builder UI (superset of MVP's Smart Rules form — see `docs/PLATFORM-FEATURES-V2.md` note that this supersedes, not parallels, the MVP rule form).
- [ ] Migration path for existing MVP `RuleSet` records into the new builder's data shape.

## Scheduled Tasks

- [ ] Implement persistent job scheduler (BullMQ-inspired, no external Redis dependency — see `docs/TECH-STACK.md`).
- [ ] Scheduled task types: nightly organization, weekly cleanup, monthly archive, backup, duplicate scans, automatic conversion.
- [ ] UI for creating/editing/disabling scheduled tasks.

## Folder Watcher (Advanced)

- [ ] Extend MVP watcher to additional sources: USB, NAS, cloud-synced folders (local path only at this phase — true cloud API integration is v2.4).
- [ ] Wire watcher events as Workflow triggers.

## Batch Processor

- [ ] Multi-operation chain execution (rename+compress+convert+move+metadata-cleanup) as a single workflow run.

## Cross-Cutting (aligned to this phase)

- [ ] File Version History (extends MVP undo/history into a full timeline with restore-previous-name).
- [ ] Clipboard Manager (track copied/moved files, paste queue, favorites).
- [ ] Performance Monitor (CPU/RAM/disk IO/FFmpeg usage/queue stats) — useful once scheduler + workflows increase background load.
